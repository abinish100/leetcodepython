class Solution(object):
    def countMatches(self, items, ruleKey, ruleValue):
        v=["type", "color", "name"]
        c=0
        for i in items:
            if i[v.index(ruleKey)]==ruleValue:
                c=c+1
        return c
