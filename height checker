class Solution(object):
    def heightChecker(self, heights):
        s=sorted(heights)
        c=0
        for i in range(len(s)):
            if s[i]!=heights[i]:
                c=c+1
        return c
        
