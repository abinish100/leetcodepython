class Solution(object):
    def countKDifference(self, nums, k):
        l=len(nums)
        c=0
        for i in range(l):
            for j in range(i,l):
                if abs(nums[i]-nums[j])==k:
                    c+=1
        return c
