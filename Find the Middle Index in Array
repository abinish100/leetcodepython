class Solution(object):
    def findMiddleIndex(self, nums):
        a=-1
        for i in range(len(nums)):
            if sum(nums[:i])==sum(nums[i+1:]):
                a=i
                break
        return a
