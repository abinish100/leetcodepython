class Solution(object):
    def findUnsortedSubarray(self, nums):
        s=sorted(nums)
        a=[]
        for i in range(len(nums)):
            if nums[i]!=s[i]:
                a.append(i)
        if not a:
            return 0
        else:
            return a[-1]-a[0]+1
