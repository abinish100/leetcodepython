class Solution(object):
    def replaceElements(self, arr):
        ma = -1
        for i in range(len(arr)-1, -1, -1):
            current = arr[i]
            arr[i] = ma
            ma= max(ma, current)
        
        return arr
