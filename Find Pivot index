class Solution(object):
    def pivotIndex(self, nums):
        j=-1
        for i in range(len(nums)-1):
            if sum(nums[:i]) == sum(nums[i:]):
                j=i
                break
        return j
