class Solution(object):
    def lastStoneWeight(self, stones):
        while len(stones) > 1:
            stones.sort()
            m1 = stones.pop()   
            m2 = stones.pop()   

            if m1 != m2:
                stones.append(m1 - m2)

        return stones[0] if stones else 0
