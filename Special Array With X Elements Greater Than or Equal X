class Solution(object):
    def specialArray(self, nums):
        m=max(nums)
        r=-1
        for x in range(m+1):
            for i in range(len(nums)):
                g=0
                for j in nums:
                    if j>=x:
                        g+=1
                if x==g:
                    r=x
                    break
        return r
