class Solution(object):
    def kthDistinct(self, arr, k):
        a=[]
        for i in arr:
            if arr.count(i) ==1:
                a.append(i)
        if len(a)<=k-1:
            return ""
        return a[k-1]
