class Solution(object):
    def canMakeArithmeticProgression(self, arr):
        s=sorted(arr)
        s1=set()
        for i in range(len(s)-1):
            s1.add(s[i]-s[i+1])
        if len(list(s1))==1:
            return True
        return False
        
        
