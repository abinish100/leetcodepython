class Solution(object):
    def countConsistentStrings(self, allowed, words):
        c = 0
        a = set(allowed)   
        for i in words:
            l = set(i)               
            if l.issubset(a): 
                c += 1
        return c
